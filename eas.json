{
  "cli": {
    "version": ">= 16.28.0",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "store",
      "environment": "development",
      "prebuildCommand": "export PATH=$PATH:/opt/homebrew/bin && cd ios && pod install"
    },
    "preview": {
      "distribution": "store",
      "environment": "preview",
      "prebuildCommand": "export PATH=$PATH:/opt/homebrew/bin && cd ios && pod install"

    },
    "production": {
      "autoIncrement": true,
      "distribution": "store",
      "environment": "production",
      "prebuildCommand": "export PATH=$PATH:/opt/homebrew/bin && cd ios && pod install"
    }
  },
  "submit": {
    "production": {}
  }
}
